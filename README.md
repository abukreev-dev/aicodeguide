<div align="center">
  <img src="https://i.imgur.com/v0Tx6am.png" alt="AI Code Guide" />
  <p align="center">
    By <a href="https://x.com/aut0mata">Vilson Vieira</a> and
       <a href="https://x.com/esrtweet">Eric S. Raymond</a>
  </p>
<br/>

> Everything you wanted to know about using AI to help you code and/or to code
> for you.

<div align="center" style="font-size: 30px">
  <a href="#vibe">TL;DR Just show me how to vibe code 😎!</a>
</div>
<br /><br />

# AI Code Guide — руководство по AI‑кодингу

**Авторы:** Vilson Vieira и Eric S. Raymond  

Всё, что вы хотели знать об использовании ИИ, чтобы помогать вам писать код и/или писать код вместо вас.

## Введение

То, как мы взаимодействуем с компьютерами и пишем для них код, меняется.  
И это глубокое изменение: и в инструментах, и в том, как мы кодим, и в том, как думаем о программных продуктах и системах.

И всё меняется очень быстро! Новые модели LLM выходят каждую неделю. Новые инструменты, новые редакторы, новые практики «vibe coding», новые протоколы — MCP, A2A, SLOP… И за всем этим очень трудно успеть. Всё раскидано по разным сайтам, репозиториям, роликам на YouTube и т.д.

Поэтому мы и решили написать это руководство. Это наша скромная попытка собрать всё вместе и показать вам практики и инструменты вокруг **AI‑кодинга** или **AI‑ассистированного написания кода** в одном месте, без лишней мишуры и в доступной форме.

- **Если вы уже умеете программировать, но ещё не пользуетесь AI‑ассистентами**, это руководство для вас: оно показывает самые свежие инструменты и хорошие практики, чтобы выжать из них максимум для ежедневной работы. Можно использовать ИИ как ваш «копилот» или наоборот — самому быть копилотом для AI‑агента.
- **Если вы никогда не кодили, но вам интересен «vibe coding», чтобы собирать свои SaaS и другие программные продукты**, это руководство тоже для вас: мы постараемся убрать лишний туман и оставить только то, что действительно важно, без «голого хайпа».

Поехали!

## AI‑кодинг? Vibe coding?

Все эти термины довольно похожи. По сути, AI‑кодинг — это использование ИИ‑моделей (сейчас в основном LLM) и всего окружения вокруг них, чтобы помогать вам писать софт. Это также называют «AI for code generation» или сокращённо «code gen», и это целое интересное поле исследований и инженерии, уходящее корнями ещё в 1950‑е, когда с помощью Lisp генерировали код. Сейчас основными двигателями генерации кода стали LLM, и уже появляются линиии исследований вокруг нейросимволических гибридных подходов.

AI‑кодинг — это ещё и практика: если вы пользуетесь Cursor и бесконечно жмёте Tab для автодополнений — вы уже «AI‑кодите»; если используете агентный режим Cursor, вы тоже «AI‑кодите». В сумме это любая форма использования ИИ‑моделей, чтобы помогать вам генерировать код. Обычно это люди, которые уже умеют кодить.

**Vibe coding** — это AI‑кодинг, выкрученный на максимум :-)  
Здесь вам не так важен конкретный код, вы просто даёте промпт и ожидаете, что ИИ всё напишет за вас. Термин придумал Карпати в 2025 году, и он стал довольно популярным. На наш взгляд, он помогает демократизировать программирование для людей, которые раньше никогда не думали о том, чтобы писать код.

Если коротко: неважно, обсуждаете ли вы с ИИ свои идеи, просите ли его помочь написать куски кода в существующем проекте или полностью «vibe‑кодите» целое приложение — вы используете ИИ для генерации кода. Давайте называть всё это **AI‑кодингом** и двигаться дальше.

## Как это использовать?

Использовать AI‑кодинг можно по‑разному, но в общем виде:

- **ИИ как копилот:** вы используете модели, чтобы расширить свои возможности и повысить продуктивность. Например, открываете чат‑модель, чтобы побрейнштормить идею для SaaS, или используете Cursor для автодополнений докстрингов. Здесь очень много плюсов, особенно для творческого исследования и автоматизации скучных частей работы.
- **ИИ как пилот:** здесь уже вы являетесь копилотом. Это и есть «vibe coding». Вы включаете, условно, «YOLO‑режим» в агенте Cursor и доверяете ему писать код. Это мощный способ «самоавтоматизации», но требует хороших практик по проектированию систем, управлению агентами и умения влезть в спагетти‑код, который вы сами толком не знаете, особенно чтобы исправлять ошибки.

Учиться и практиковать нужно оба режима!  
Но по мере роста сложности проекта лучше смещаться в сторону **копилотирования** и уходить от чистого YOLO‑vibe‑кодинга. Чем выше шанс, что другой человек (или вы сами через полгода) будет поддерживать этот код, тем это важнее.

## 🗺️ Дорожная карта

### С чего начать?

- Если вы не умеете кодить и хотите просто поиграться, начните с веб‑платформ типа Bolt, Replit, v0 или Lovable.
- Если вы уже умеете кодить, установите Cursor или Windsurf. Можно начать с бесплатного плана, затем перейти на платный. Cursor сейчас довольно хорош и дешёв по соотношению «цена/количество токенов и свежесть моделей». У VSCode недавно появился свой Agent Mode, который работает вместе с GitHub Copilot и использует агентный рабочий процесс для изменений и правок файлов. Другие редакторы быстро добавляют агентные функции, проверьте сайт своего любимого редактора.
- Если хотите более открытое решение, попробуйте OpenHands. Он запускается как Docker‑контейнер с web‑интерфейсом. Понадобится аккаунт Anthropic и API‑ключ, либо модель через OpenRouter.
- Если вы любите терминал, посмотрите на aider, Claude Code или OpenAI Codex CLI. Для них нужны API‑ключи Anthropic Claude, OpenAI GPT или OpenRouter.
- Если вам нравится и терминал, и «VScode‑подобный» интерфейс, и хочется поэкспериментировать с параллельными субагентами, а на стоимость вы смотрите меньше, чем на результат — попробуйте Amp.

**Совет.** Очень рекомендуем завести аккаунт на OpenRouter — это просто, и вы получите доступ к самым свежим моделям LLM, зачастую с бесплатными квотами. Например, многие использовали Gemini 2.5 Pro через OpenRouter бесплатно (с ежедневным лимитом).

Важно: Claude Code сейчас очень дорогой. Можно легко «сжечь» 50 $/день. Поэтому начинать лучше с Cursor, чтобы не переживать за счет.

### Как промптить для кодинга (как «vibe‑кодить»)?

Когда вы немного поиграете с инструментами, заметите, что они иногда галлюцинируют, заходят в бесконечные циклы «починки» ошибок и т.д. Поэтому важно уметь правильно формулировать запросы.

Основные принципы:

- Не просите всё сразу одним промптом.  
  Фраза уровня «Эй, сделай мне приложение для зоомагазина» не поможет ни инженеру, ни ИИ. Сначала разберитесь с идеей, побрейнштормьте с LLM, создайте PRD (Product Requirements Document), сделайте план и разбейте его на задачи.
- Давайте детали.  
  Если вы знаете стек, язык, предполагаемую аудиторию, ограничения — обязательно добавляйте это в промпт.
- Используйте Markdown или другой лёгкий текстовый формат (asciidoc и т.п.).  
  В итоге всё равно всё превращается в токены, но формат помогает структурировать мысль и фокус.
- Разбивайте проект на задачи и подзадачи.
- Пробуйте разные модели для разных целей и сверяйте их ответы между собой.
- Помните, что LLM — «машины согласия»: они стремятся угодить, а не спорить. Нужно включать критическое мышление.

Дальше мы предлагаем рабочий метод (workflow), который обычно даёт хороший результат.

#### 1. Генерация PRD

Используйте продвинутую модель (в оригинале авторы упоминают ChatGPT 4.5, 4o, o3, но вы можете выбрать актуальный аналог) и дайте ей промпт вида:

> Ты — сеньор‑разработчик. Мы вместе будем писать PRD для проекта.  
> Очень важно:
> - Задавай по одному вопросу за раз.
> - Каждый следующий вопрос должен опираться на предыдущие ответы.
> - Углубляйся в каждый важный аспект.
>
> ИДЕЯ:  
> \<сюда вставьте свою идею\>

Вы входите в «цикл вопросов‑ответов» на несколько минут. Отвечайте максимально подробно. Когда решите, что информации достаточно, попросите модель:

> Скомпилируй все наши находки в PRD в Markdown.  
> Он должен включать:
> - Обзор проекта
> - Основные требования
> - Основные функции
> - Основные компоненты
> - Пользовательский/приложенческий флоу
> - Техстек
> - План реализации

Сохраните этот файл как `docs/specs.md` в корне проекта.

#### 2. Генерация плана задач

Дальше попросите:

> На основе PRD создай детальный пошаговый план реализации.  
> Разбей на задачи и подзадачи, без резких скачков по сложности.  
> Используй формат чек‑листов, пронумеруй задачи, для каждой укажи зависимости.

Сохраните это как `docs/todo.md`.

Теперь у вас есть PRD и список задач. Откройте свой AI‑редактор (например, Cursor), подключите к нему эти файлы и дайте инструкцию:

> Ты — сеньор‑разработчик. Изучи `docs/specs.md` и реализуй то, чего нет в `docs/todo.md`.  
> Выполняй задачи по порядку, уважая зависимости.  
> После завершения задачи отмечай её в списке как выполненную и переходи к следующей.

При первой команде агента можете включить «YOLO‑режим», а дальше уже внимательно просматривать меняемые файлы и либо принимать, либо просить «продолжить».

## Как запускать проект и с чем его сочетать

### Веб‑приложение (Frontend)

Современная веб‑разработка перегружена: десятки фреймворков JS/TS, CSS‑библиотек и т.д. Пара рабочих вариантов:

- Next.js, чтобы легко деплоить на Vercel (экосистема удобная, но может стать дорогой).
- «Ванильный» React с React Router, чтобы деплоить куда угодно.
- Remix — с удобным роутингом и возможностью деплоя на разные платформы.
- FastHTML, если вы любите Python и больше заинтересованы в функциональности бэкенда (анализ данных, ML‑пайплайны), чем в суперкрутом UI.

Многие веб‑платформы AI‑кодинга (типа Lovable) используют React, а v0 — Next.js.

Обычно удобно начать проект на Lovable, настроить выгрузку проекта в GitHub, а дальше клонировать к себе и продолжать AI‑кодинг в Cursor. Деплой — на Render, Fly.io, Cloudflare и т.п.

Если вы пишете и фронтенд, и бэкенд, держите их либо в разных папках, либо в разных репозиториях, но в одном рабочем пространстве редактора, чтобы агент мог ссылаться на файлы обеих частей.

Чтобы не склеивать фронтенд и бэкенд слишком рано, попросите агента использовать заглушки/моки вместо реального API, а когда бэкенд будет готов — заменить их вызовами настоящих эндпоинтов.

Если хотите 3D‑контент в React‑приложении, лучше использовать React Three Fiber, а не напрямую three.js — так проще управлять состоянием через компоненты.

### Бэкенд

Веб‑платформы вроде Lovable часто слабее в части бэкенда, поэтому обычно лучше использовать локальные инструменты (Cursor, Windsurf, aider и т.п.).

Хорошие варианты:

- Python + FastAPI.
- Node.js + Express, если хотите оставаться в одном языке с фронтендом.

Бэкенд — отличное место для end‑to‑end тестов. Направляйте агента писать тесты и запускать их при добавлении фич.

Когда бэкенд готов, используйте его документацию (особенно описание HTTP‑эндпоинтов) как вход для агента, работающего над фронтендом, чтобы заменить моки на реальные запросы.

### Игры

Для небольших игр подойдёт:

- Ванильный JS в одном `.js`‑файле.
- three.js — для 3D.
- pixi.js — для 2D.

Игры сильно зависят от ассетов, поэтому логично использовать сервисы вроде Tripo AI или Anything World для генерации и анимации 3D‑моделей.

## Как разбираться с ошибками и багами

Ошибки в софте неизбежны. Важно это принять и подружиться с ними.

Базовая стратегия:

- Посмотрите на сообщение об ошибке от интерпретатора/компилятора и попытайтесь его понять.
- Скопируйте ошибку и вставьте её в LLM, попросите объяснить и исправить.
- Очень полезно подключать инструменты для дебага (например, браузерные отладчики, интеграцию с playwright/browser‑use и т.п.), чтобы агент сам собирал ошибки и скриншоты.

## MCP, SLOP и A2A — что это и зачем?

**MCP (Model Context Protocol)** — протокол от Anthropic, который начинает поддерживаться и другими вендорами (OpenAI, Google). Это способ стандартизировать взаимодействие LLM с внешними ресурсами и инструментами.

**Tool calling** — механизм, с помощью которого LLM может запрашивать внешние операции (поиск, доступ к БД, вызовы API) через заранее описанные «инструменты». Это позволяет обновлять контекст модели актуальной информацией и интегрировать её с системами.

MCP расширяет эту идею и задаёт единый стандарт: можно поднять MCP‑сервер, который отдаёт ресурсы (например, базу данных) или инструменты (сервер, считающий что‑то и возвращающий результат) в понятном LLM формате.

**SLOP (Simple Language Open Protocol)** — альтернативный подход, ближе к классическому REST, где интерфейсы описываются текстом и протокол проще.

**A2A (Agent‑to‑Agent Protocol)** — новый протокол от Google, «дополняющий» MCP, но уже для общения между агентами (multi‑agent системы), тогда как MCP focuses on «LLM ↔ инструменты».

На сегодняшний день MCP нативно поддерживается в основном Claude, но редакторы типа Cursor уже умеют подключать MCP‑серверы.

## Начинать с нуля или с boilerplate?

Часто LLM работают лучше, когда стартуют «с нуля». Но можно и начать с готового шаблона (boilerplate): минимальный каркас проекта под конкретный стек.

Полезные практики:

- Задать правила/конвенции и включить их в контекст (например, через `.cursor/rules` или `rules.md` для aider).
- Индексировать проект (в Cursor, через repomix или files‑to‑prompt), чтобы агент ориентировался в структуре и коде.

## Чистый проект vs существующий код

Если у вас уже есть кодовая база:

- Используйте repomix или files‑to‑prompt, чтобы упаковать проект в контекст.
- Просите изменения на уровне задач/фич, а не «почини весь проект».  
  Опишите мини‑PRD для конкретной фичи, как если бы вы ставили задачу джуниору по тикету в issue‑трекере.

## Структурированное проектирование и промптинг

Состоянием на 2025 год LLM неплохо пишут рабочий код, но плохо сами по себе проектируют архитектуру и структуры. Хорошая архитектура важна для читабельности, сопровождения и снижения количества багов.

Рекомендации:

- Сначала продумайте дизайн, потом уже промптинг.  
  Например, в базе‑центричных приложениях сначала задайте типы записей (модели), затем слой доступа (manager/репозитории), и только потом бизнес‑логику.
- Разделяйте «движок» от «политики» (engine vs policy): просите LLM сначала определять низкоуровневые абстракции и только потом «правила» и сценарии.
- Явно добавьте в правила проекта запрет на нарушение слоёв (чтобы низкоуровневые детали не пролезали прямо в бизнес‑логику).

Можно даже сначала написать классы/функции‑«скелеты» и попросить модель заполнить «мясо».

## Тесты: TDD и не только

Тесты важны как никогда.  

LLM иногда генерируют чистый и вроде бы корректный код, но могут:

- неправильно понять спецификацию;
- «подгонять» реализацию под тесты (вплоть до хардкода).

Это не исчезнет даже при полностью «человеческом» ИИ: люди точно так же иногда неправильно понимают требования.

Рекомендации:

- Используйте TDD там, где это оправдано: сначала тесты/спецификации, потом код.
- Просите LLM писать тесты и запускать их; включайте сообщения об ошибках в контекст.
- Внедряйте property‑based тестирование (Hypothesis для Python, fast‑check для JS/TS и т.д.) для проверки целых диапазонов входов, а не только нескольких примеров.
- Внимательно проверяйте тесты, которые пишет LLM — иногда он может «обмануть» тест, а не исправить код.

Тесты — скелет, на который вы наращиваете кодовую базу вместе с LLM, двигаясь вперёд только при «зелёных» тестах.

## Безопасность

Правила те же, что и в обычной разработке, только их выполнение сложнее контролировать, когда код генерирует ИИ.

Чек‑лист:

- Не доверяйте коду «из коробки» — всегда проверяйте. Ответственность за запущенный код всё равно на вас.
- Никогда не храните API‑ключи и другие секреты в коде (особенно на фронтенде). Используйте переменные окружения и секрет‑хранилища платформ деплоя.
- Всегда используйте HTTPS при запросах к API.
- Для HTML‑форм обязательно делайте валидацию и санитизацию данных.
- Не храните чувствительные данные в `localStorage`, `sessionStorage` или куках.
- Регулярно прогоняйте линтеры, форматтеры и сканеры уязвимостей.

## Глобальные правила проекта

Вы можете задавать правила/конвенции, которые будут применяться ко всему проекту, «впрыснув» их в контекст LLM.

Примеры:

- В Cursor: создать файлы правил в `.cursor/rules/`, которые редактор будет автоматически подмешивать во все запросы.
- В aider: создать `rules.md` и подключить его в `.aider.conf.yml` через `read: rules.md`.
- Есть и глобальные файлы правил в домашнем каталоге (например, `~/.global_conventions.md`), которые можно подключать ко всем проектам.

В эти правила можно включать части PRD (например, техстек, требования к стилю кода и т.п.). Иногда полезно попросить саму LLM сгенерировать для вас набор правил по проекту.

## Журнал промптов

Записывайте:

- все промпты;
- свои мысли между ними;
- неожиданные результаты и инсайты.

Такой лог — запись проектных намерений, очень полезная для вас и других разработчиков, которые будут разбираться в коде. Имя файла произвольное: `vibecode.adoc`, `history.md` и т.п.

Некоторые инструменты (как aider) уже ведут такие журналы автоматически (`.aider.input.history`, `.aider.chat.history.md`, `.aider.llm.history` и др.). Держите их под версионированием и комментируйте по ходу работы — так вы учитесь у собственной истории.

---

